[project]
name = "youtube-downloder"
version = "0.1.0"
description = "YouTube video and playlist downloader"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.2",
    "yt-dlp>=2025.12.8",
]
